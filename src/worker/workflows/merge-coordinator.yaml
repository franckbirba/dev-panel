name: merge-coordinator
description: Auto-review and merge PRs — triggered by GitHub webhook or internal pipeline.
max_revisions: 2
on_exhaustion: block

steps:
  - agent: merge-coordinator
    on:
      done:     { terminal: true }
      blocked:  { terminal: true }
      failed:   { terminal: true }
